version: '3.8'

services:
  # Dashboard principal (apenas no manager)
  dashdocker:
    image: dashdocker:latest
    build:
      context: .
      target: production
    hostname: dashdocker
    ports:
      - "8080:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - WAHA_URL=${WAHA_URL}
      - WAHA_TOKEN=${WAHA_TOKEN}
      - WAHA_SESSION=${WAHA_SESSION}
      - WAHA_PHONE=${WAHA_PHONE}
      - NODE_ENV=production
      - DASHBOARD_USERNAME=${DASHBOARD_USERNAME:-admin}
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-admin123}
      - VITE_API_URL=
    networks:
      - dashdocker-network
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
      placement:
        constraints:
          - node.role == manager
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        monitor: 60s
        max_failure_ratio: 0.3
      rollback_config:
        parallelism: 1
        delay: 5s
        monitor: 60s
        max_failure_ratio: 0.3

  # Agent de monitoramento (todos os nodes)
  dashdocker-agent:
    image: dashdocker-agent:latest
    build:
      context: ./agent
      dockerfile: Dockerfile
    environment:
      - BACKEND_URL=ws://dashdocker:3001
      - NODE_NAME={{.Node.Hostname}}
      - AGENT_UPDATE_INTERVAL=5000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - dashdocker-network
    deploy:
      mode: global  # Roda em TODOS os nodes
      resources:
        limits:
          cpus: '0.1'
          memory: 64M
        reservations:
          cpus: '0.05'
          memory: 32M
      restart_policy:
        condition: on-failure
        delay: 10s  # Aumentar delay para dar tempo do backend subir
        max_attempts: 5
        window: 300s  # Aumentar janela de tempo

networks:
  dashdocker-network:
    driver: overlay
    attachable: true
